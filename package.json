{
  "name": "thinkful-full-stack-template",
  "version": "0.0.1",
  "description": "A full-stack template for Thinkful students",
  "main": "index.js",
  "main:server": "server/index.js",
  "main:client": "client/js/index.js",
  "scripts": {
    "start": "[ \"$NODE_ENV\" = \"production\" ] && npm run serve:server:production || npm run serve:server:dev",
    "dev": "run-p watch serve:server:dev",
    "test": "mocha --recursive",
    "repl": "babel-node",
    "build": "run-s build:*",
    "clean": "rm -rf build",
    "serve": "npm start",
    "watch": "run-s watch:*",
    "heroku-postbuild": "npm run build",
    "clean:server": "rm -rf build/server",
    "mkdir:server": "mkdir -p build/server",
    "build:server": "run-s build:server:production",
    "serve:server:dev": "NODE_ENV=development CLIENT_PATH=build/dev/client nodemon --exec babel-node server/index.js --watch server --ignore server/public",
    "clean:server:production": "rm -rf build/production/server",
    "mkdir:server:production": "mkdir -p build/production/server",
    "build:server:production": "run-s clean:server:production mkdir:server:production && NODE_ENV=production babel server -d build/production/server --ignore public",
    "serve:server:production": "NODE_ENV=production CLIENT_PATH=build/production/client node build/production/server/index.js",
    "clean:client": "run-s clean:client:*",
    "mkdir:client": "run-s mkdir:client:*",
    "build:client": "run-s build:client:*",
    "watch:client": "run-s watch:client:dev",
    "clean:client:dev": "rm -rf build/dev/client",
    "mkdir:client:dev": "mkdir -p build/dev/client",
    "build:client:dev": "run-s build:client:dev:*",
    "watch:client:dev": "run-p watch:client:dev:*",
    "clean:client:dev:html": "rm -f build/client/dev/index.html",
    "mkdir:client:dev:html": "run-s mkdir:client:dev",
    "build:client:dev:html": "run-s clean:client:dev:html mkdir:client:dev:html && mustache package.json client/index.html > build/dev/client/index.html",
    "watch:client:dev:html": "run-s build:client:dev:html && chokidar client/index.html -c 'npm run build:client:dev:html'",
    "clean:client:dev:js": "rm -rf build/dev/client/js",
    "mkdir:client:dev:js": "mkdir -p build/dev/client/js",
    "build:client:dev:js": "run-s clean:client:dev:js mkdir:client:dev:js && NODE_ENV=development webpack",
    "watch:client:dev:js": "run-s build:client:dev:js && NODE_ENV=development webpack --watch",
    "clean:client:dev:css": "rm -rf build/dev/client/css",
    "mkdir:client:dev:css": "mkdir -p build/dev/client/css",
    "build:client:dev:css": "run-s clean:client:dev:css mkdir:client:dev:css && node-sass --source-map true client/scss/index.scss build/dev/client/css/$npm_package_name.$npm_package_version.css",
    "watch:client:dev:css": "run-s build:client:dev:css && NODE_ENV=development node-sass --source-map true -wr client/scss/index.scss build/dev/client/css/$npm_package_name.$npm_package_version.css",
    "clean:client:dev:assets": "rm -rf build/dev/client/assets",
    "build:client:dev:assets": "run-s clean:client:dev:assets mkdir:client:dev && cp -r client/assets build/dev/client",
    "watch:client:dev:assets": "run-s build:client:dev:assets && chokidar client/assets/**/* -c 'npm run build:client:dev:assets'",
    "clean:client:production": "rm -rf build/production/client",
    "mkdir:client:production": "mkdir -p build/production/client",
    "build:client:production": "run-s build:client:production:*",
    "clean:client:production:html": "rm -f build/production/client/index.html",
    "mkdir:client:production:html": "run-s mkdir:client:production",
    "build:client:production:html": "run-s clean:client:production:html mkdir:client:production:html && { echo '{\"production\": true}'; cat package.json; } | json --merge | mustache - client/index.html > build/production/client/index.html",
    "clean:client:production:js": "rm -rf build/production/client/js",
    "mkdir:client:production:js": "mkdir -p build/production/client/js",
    "build:client:production:js": "run-s clean:client:production:js mkdir:client:production:js && NODE_ENV=production webpack",
    "clean:client:production:css": "rm -rf build/production/client/css",
    "mkdir:client:production:css": "mkdir -p build/production/client/css",
    "build:client:production:css": "run-s clean:client:production:css mkdir:client:production:css && node-sass --source-map true client/scss/index.scss build/production/client/css/$npm_package_name.$npm_package_version.css && cleancss --source-map --output build/production/client/css/$npm_package_name.$npm_package_version.min.css build/production/client/css/$npm_package_name.$npm_package_version.css",
    "clean:client:production:assets": "rm -rf build/production/client/assets",
    "build:client:production:assets": "run-s clean:client:production:assets && cp -r client/assets build/production/client"
  },
  "author": "Joe Turner <joe@oampo.co.uk>",
  "license": "UNLICENSED",
  "devDependencies": {
    "babel-cli": "^6.11.4",
    "babel-loader": "^6.2.4",
    "babel-plugin-transform-object-rest-spread": "^6.8.0",
    "babel-polyfill": "^6.13.0",
    "babel-preset-es2015": "^6.13.1",
    "babel-preset-react": "^6.11.1",
    "chokidar-cli": "^1.2.0",
    "clean-css": "^3.4.19",
    "json": "^9.0.4",
    "mocha": "^3.0.1",
    "mustache": "^2.2.1",
    "node-sass": "^3.8.0",
    "nodemon": "^1.10.0",
    "npm-run-all": "^3.0.0",
    "webpack": "^1.13.1"
  },
  "dependencies": {
    "express": "^4.14.0",
    "isomorphic-fetch": "^2.2.1"
  }
}
